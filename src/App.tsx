import AppRoutesConfig from "./core/routes";
import AppThemeConfig from "./core/theme";

function App() {
  return (
    <AppThemeConfig>
      <AppRoutesConfig />
    </AppThemeConfig>
  );
}

export default App;
